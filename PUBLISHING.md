# 公开发布清单

## 发布阻断项

- [ ] 已撤销曾出现在历史中的第三方 Cookie 与 API Key。
- [ ] 已使用 Gitleaks 或同类工具扫描全部 Git 历史。已完成常见凭据模式的流式历史扫描；GitHub 首次推送后仍需确认 Gitleaks Action 通过。
- [x] 已确认 `~/.lianxin`、`data/`、`memory/`、`logs/` 和用户配置未被 Git 跟踪。
- [ ] 已完成音乐、GIF、视觉模型等随仓库发布资源的来源和许可证清单；AI 生成的视觉、角色和音乐素材及 Pixabay 音效已登记，`vision/models/` 的第三方模型许可证仍待核验。表情包由用户本地自行配置，不随仓库发布。
- [x] 已选择并加入项目许可证：源代码采用 MIT License；素材和模型仍按 `ASSET_NOTICE.md` 单独核验。

## 首发前完善项

- [x] README 包含安装步骤、系统要求、配置说明、隐私说明和功能边界。
- [x] 将核心依赖与语音、视觉、QQ/微信、MCP 等可选依赖拆分。
- [x] 已筛选离线具身模拟回归测试，并配置 GitHub Actions 执行语法检查、回归、依赖审计和凭据扫描。
- [x] 已添加 Dependabot 依赖更新检查与发布前检查入口。
- [x] 已在隔离 Python 3.12 环境与空用户目录中运行 `bootstrap.bat` 验收：基础依赖安装、配置初始化、无 API Key 默认状态和 `main` 导入均通过。首发前仍应在有桌面会话的干净 Windows 环境中手动确认 `run.bat` 的 GUI 首次显示。

## Git 历史瘦身

历史改写需要先处理真实凭据，再使用 `git filter-repo` 清除已失效 Cookie 和不再发布的大型资源。改写后应以干净克隆验证启动，再强制推送远端。协作者需要重新克隆或基于新历史同步。

2026-07-31 审计：`git fsck --full` 通过；常见凭据模式在排除示例占位符后未命中。旧历史中的 `ocr/libtesseract-5.dll`（约 96.8 MiB）不进入 GitHub：首发使用仅包含当前已审查文件的干净公开历史，保留既有 Gitee 历史不改写。`vision/models/` 也不随仓库发布，改由本地下载指引提供。