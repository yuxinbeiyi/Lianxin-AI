# 莲心肩载设备人脸追踪方案

## 1. 目标

让莲心使用 ESP32-CAM 肩载摄像头持续推流，在电脑端完成本人脸检测与识别，并通过 Pan/Tilt 舵机让人脸尽量保持在视频画面中央。

电脑端视频窗口显示：

- ESP32-CAM 实时画面
- 本人人脸检测框
- 人脸中心点
- 视频中心十字线
- 从视频中心指向人脸中心的误差矢量
- 当前跟踪状态、FPS、Pan/Tilt 角度和识别延迟

ESP32-CAM 只负责摄像头采集、JPEG 推流和舵机执行，不负责做人脸识别或绘制检测框。

## 2. 总体闭环

```text
ESP32-CAM 采集 JPEG
        |
        v
WebSocket 云中继
        |
        v
电脑端接收最新帧
        |
        v
InsightFace 检测并识别本人
        |
        v
计算人脸中心相对画面中心的误差
        |
        v
比例控制 + 死区 + 平滑 + 短时预测
        |
        v
发送 Pan/Tilt 目标角度
        |
        v
ESP32 平滑驱动舵机
        |
        +------ 下一帧重新检测，持续修正 ------+
```

这是一个连续反馈系统，不采用“检测一次后转到固定角度”的方式。

## 3. 现有代码复用

### ESP32-CAM

复用 `E:\Desktop\莲心外设\esp32-cam\src\main.cpp`：

- `track_start`：开始发送 JPEG 帧
- `track_stop`：停止发送 JPEG 帧
- `servo <pan> <tilt>`：设置两个舵机目标角度
- ESP32 端已有舵机平滑运动逻辑

第一阶段不改变 ESP32 的视频协议和摄像头固件。

### 电脑端通信

复用 `brain/hardware_bridge.py`：

- 建立一条长期 WebSocket 连接
- 接收 ESP32 推送的二进制 JPEG
- 同一连接发送 `track_start`、`servo` 和 `track_stop`
- 结束时发送 `servo 90 45`，让云台回中

不能在每一帧重新建立连接，也不能使用普通拍照工具的短连接模式。

### 视觉识别

复用 `VisionLab/app/features/face_feature.py` 的 InsightFace 能力：

- 获取人脸框 `bbox`
- 获取检测置信度
- 通过人脸特征判断是否为本人
- 在电脑端画出检测框

现有 `workers/track_worker.py` 的 WebSocket 接收、舵机发送和生命周期管理逻辑可以参考或重构，但检测器从 MediaPipe Pose 改为 InsightFace 人脸检测。

## 4. 人脸目标选择

优先选择身份为 `USER` 的人脸：

1. 画面中识别到本人时，只跟踪本人。
2. 画面中有多人但没有本人时，不移动云台。
3. 本人身份暂时不确定时，进入短暂确认状态，不立即跟踪陌生人。
4. 连续识别到本人若干帧后才进入正式跟踪，避免单帧误检。

建议初始参数：连续确认 3 帧。

## 5. 坐标和误差矢量

设视频尺寸为 `width x height`，则：

```text
视频中心 C = (width / 2, height / 2)
人脸框     = (x1, y1, x2, y2)
人脸中心 F = ((x1 + x2) / 2, (y1 + y2) / 2)
误差向量 E = F - C
```

例如人脸位于画面左上方，则 `E.x < 0` 且 `E.y < 0`。控制器根据误差方向调整 Pan/Tilt。

视频窗口直接绘制 `C -> F` 的矢量线，作为控制误差的可视化。

注意：图像坐标的 Y 轴向下，而数学坐标系的 Y 轴通常向上。内部统一使用图像坐标，最终由 `TILT_DIRECTION` 配置项处理方向转换。

## 6. 动态闭环控制

### 6.1 比例控制

不把人脸坐标直接映射为舵机绝对角度，而是根据当前误差逐步修正：

```text
Pan 调整量  = error_x / width  * PAN_GAIN
Tilt 调整量 = error_y / height * TILT_GAIN
```

误差越大，调整步长越大；越接近中心，调整越小。

Pan/Tilt 每次发送的是新的目标角度，ESP32 负责在目标角度之间平滑移动。

### 6.2 中心死区

画面中心附近设置死区，避免舵机在中心位置抖动：

```text
abs(error_x) < width  * 0.08
abs(error_y) < height * 0.08
```

误差落在死区内时，该轴不发送新的调整命令。

### 6.3 速度和角度限制

- 电脑端舵机命令间隔初始设置为 80~120 ms。
- 单次最大角度调整量必须限制。
- Pan/Tilt 设置机械安全范围，初始建议沿用现有保守范围。
- 方向由 `PAN_DIRECTION`、`TILT_DIRECTION` 配置，首次测试时分别校准。
- 不直接使用理论 0~180 度作为机械运行范围，除非确认云台不会撞限位。

### 6.4 最新帧策略

接收队列只保留最新的 1~2 帧：

```text
新帧到达 -> 丢弃旧帧 -> 视觉线程处理最新帧
```

不能排队处理全部视频帧，否则识别结果会落后于真实位置，出现“人已经移动到右下角，舵机还在追左上角”的问题。

## 7. 移动目标的延迟补偿

人脸移动时，系统会受到网络、JPEG 解码、人脸识别和舵机响应延迟影响。为减小滞后，记录最近两次或多次人脸中心位置：

```text
速度 X = 当前中心 X - 上次中心 X
速度 Y = 当前中心 Y - 上次中心 Y
```

对速度做指数平滑后，计算短时间预测位置：

```text
预测中心 = 当前中心 + 平滑速度 * 预测时间
```

建议初始预测时间为 100~200 ms，并限制最大预测偏移。

预测只在以下条件同时满足时启用：

- 连续检测到本人
- 最近几帧位置变化稳定
- 没有发生目标切换

目标突然消失、身份变化或检测框跳变时，立即清空速度估计，避免过冲。第一版可以先关闭预测，只使用低延迟最新帧和比例控制，确认基本闭环稳定后再启用预测。

## 8. 丢脸和异常保护

建议状态流程：

```text
IDLE -> CONFIRMING -> TRACKING -> LOST_GRACE -> IDLE
```

- `IDLE`：未启动。
- `CONFIRMING`：连续确认本人。
- `TRACKING`：正常闭环追踪。
- `LOST_GRACE`：短暂丢脸，保持云台当前位置。

初始保护参数：

- 丢失小于 300 ms：保持当前控制状态。
- 丢失 300 ms~1.5 s：停止发送新的移动命令，云台保持当前位置。
- 丢失超过 1.5 s：退出跟踪或进入待机，不继续盲目转动。
- 默认不自动扫描，避免摄像头转向不可控方向。

其他保护：

- ESP32 连接断开时立即停止控制循环。
- 线程退出时发送 `track_stop`。
- 线程退出时尽力发送回中命令。
- 识别异常不能导致舵机继续沿上一次速度运动。
- 所有角度和命令频率都必须经过限制。

## 9. 软件模块设计

建议新增或重构为以下职责：

### `FaceTrackSession`

负责启动、停止、线程生命周期和整体状态。

### `RemoteFrameReceiver`

负责长期 WebSocket 连接、发送 `track_start`、接收二进制 JPEG，并只保留最新帧。

### `FaceTracker`

负责调用 InsightFace、筛选本人、输出检测框、中心点、置信度和速度估计。

### `GimbalController`

负责死区、比例控制、预测、方向校准、角度限制和命令限频。

### `FaceTrackingWindow`

负责显示远程视频、检测框、中心十字、误差矢量和调试状态。

## 10. 莲心工具接口

新增两个主要工具：

```text
shoulder_face_track
    启动肩载摄像头本人脸实时追踪

stop_face_tracking
    停止人脸追踪、关闭视频窗口、停止推流并让云台回中
```

原来的 `shoulder_human_track` 不再作为主要入口。由于人体跟踪功能已经弃用，后续应同步清理或标记以下旧内容：

- `shoulder_human_track` 工具描述
- `workers/track_worker.py` 中的 Pose 检测逻辑
- 人体跟踪状态管理和旧路由名称
- `SKILL.md` 中“人体跟踪”的说明

清理应在新的人脸追踪功能稳定后进行，避免调试阶段失去可参考代码。

## 11. 分阶段开发

### 阶段一：只显示远程视频

- 建立长期连接。
- 接收 ESP32 JPEG。
- 电脑窗口稳定显示视频。
- 验证最新帧策略和实际 FPS。

### 阶段二：显示人脸框和矢量

- 调用 InsightFace。
- 识别本人。
- 绘制人脸框、中心点、画面中心和误差矢量。
- 暂不控制舵机。

### 阶段三：单轴闭环

- 只启用 Pan。
- 校准左右方向。
- 调整死区、增益和命令间隔。
- 确认人脸横向移动时没有明显振荡。

### 阶段四：双轴闭环

- 加入 Tilt。
- 校准上下方向。
- 调整 Pan/Tilt 的独立增益和安全范围。

### 阶段五：动态优化

- 加入速度平滑。
- 加入短时预测。
- 完善丢脸保护、断线处理和窗口状态显示。

### 阶段六：工具和产品化

- 注册 `shoulder_face_track` 和 `stop_face_tracking`。
- 加入莲心路由和 skill 描述。
- 完善启动、停止、重复启动和异常退出行为。
- 移除或停用旧人体跟踪入口。

## 12. 验收标准

功能达到以下条件后，才认为第一版完成：

- 视频窗口能稳定显示 ESP32-CAM 画面。
- 检测框能跟随本人，而不是跟随陌生人。
- 人脸位于画面左侧时，云台能向正确方向修正。
- 人脸从左上移动到右下时，系统处理的是最新位置，不会明显追踪旧位置。
- 人脸接近中心时，舵机停止抖动。
- 短暂遮挡不会立即导致云台乱转。
- 长时间丢脸后舵机停止继续追踪。
- 停止工具可以关闭推流、关闭窗口并让云台回中。
- ESP32 断线时莲心能够给出明确错误，不会卡死主界面。

## 13. 关键设计结论

1. 人脸识别和控制全部放在电脑端，ESP32 保持简单可靠。
2. 使用长期 WebSocket 连接承载视频接收和舵机控制。
3. 使用误差矢量进行连续比例控制，而不是一次性角度映射。
4. 只处理最新视频帧，优先保证实时性而不是处理完整帧序列。
5. 通过死区、限频、角度限制和丢脸状态机保证稳定与安全。
6. 先完成无预测的稳定闭环，再加入运动预测降低跟踪滞后。
7. 新功能稳定后，再清理已弃用的人体 Pose 跟踪代码。

## 14. 当前实现进度

截至 2026-08-25，第一版电脑端闭环已经写入项目：

- `brain/face_tracking.py` 已实现长期 WebSocket、JPEG 接收、InsightFace 本人筛选、调试视频窗口和舵机控制。
- `shoulder_face_track` 与 `stop_face_tracking` 已接入肩部外设 skill、工具路由和资源分组。
- `GimbalController` 已从通信线程中抽出，控制逻辑可以脱离硬件单独测试。
- `tests/test_face_tracking_control.py` 已覆盖死区、限频、方向反转、最大步长和角度边界。
- 当前离线控制测试结果为 `5 passed`。

仍需回家连接实际 ESP32-CAM 后验证：

1. ESP32 是否能稳定推送 JPEG 帧。
2. Pan/Tilt 正负方向是否与实际云台方向一致。
3. 增益、死区、预测时间是否适合实际舵机和机械结构。
4. 网络延迟、舵机响应和人脸移动速度下是否需要进一步调参。

## 15. 分层性能排查方案

当前实机测试中，视频已经能够显示，但画面明显卡顿。为了区分网络推流、视频显示、手势推理和 InsightFace 推理各自的影响，后续按照以下顺序逐层增加处理环节。每一阶段都应单独记录实际 FPS、画面是否逐渐卡死、延迟是否持续增加以及停止后是否能够正常释放资源。

### 15.1 阶段 A：ESP32 纯推流

这一阶段只执行：

```text
ESP32-CAM 采集 JPEG
    -> 云中继转发
    -> 电脑端 WebSocket 接收
    -> JPEG 解码
    -> Qt 窗口显示
```

暂时关闭以下内容：

- InsightFace 人脸检测和人脸识别；
- 手势识别；
- 舵机控制；
- 人脸框、中心点和误差矢量绘制。

纯推流测试窗口应显示以下指标：

- 接收 FPS；
- 界面显示 FPS；
- 最近一帧 JPEG 的尺寸；
- 已运行时间；
- 最近帧序号或帧更新时间。

连续运行至少 3~5 分钟。若纯推流仍卡顿，优先检查 ESP32 的 JPEG 分辨率、推流间隔、云中继转发和电脑端解码；若纯推流稳定，则说明主要瓶颈在电脑端视觉推理或结果绘制。

### 15.2 阶段 B：纯推流加手势识别

在阶段 A 稳定后，将同一批远程视频帧交给现有 `GestureFeature.process(frame)`，但不启用人脸识别和舵机控制。

```text
最新远程帧
    -> 手势检测
    -> 手势分类
    -> 绘制手部关键点
    -> 显示窗口
```

手势算法本身可以复用现有 VisionLab 实现，不重新编写分类器。需要新增的只是远程帧适配层，并且必须继续采用“只处理最新帧”的策略，不能建立未限制长度的识别队列。

通过标准：手势识别开启后，视频仍能持续刷新；即使手势推理速度低于接收速度，也不能积压旧帧。

### 15.3 阶段 C：纯推流加 InsightFace

关闭手势识别，只加入 InsightFace 人脸检测和本人特征匹配，不发送舵机命令。

重点记录：

- InsightFace 实际推理 FPS；
- CPU 或 CUDAExecutionProvider；
- 单次推理耗时；
- 视频显示 FPS；
- 界面延迟是否随时间增加。

该阶段用于确认卡顿是否来自 InsightFace。GPU Provider 应以日志中的 `CUDAExecutionProvider` 为准，不能只根据任务管理器的瞬时 GPU 利用率判断。

### 15.4 阶段 D：加入人脸框和误差矢量

在人脸识别稳定后，再加入人脸框、视频中心点、人脸中心点和误差箭头。绘制操作只对界面当前取出的最新帧执行，不通过 Qt 信号逐帧排队传递图像。

如果阶段 C 流畅而阶段 D 卡顿，应检查重复绘制、QImage/QPixmap 转换和界面缩放，而不是继续调整识别模型。

### 15.5 阶段 E：加入舵机闭环

确认阶段 D 稳定后，最后才发送 Pan/Tilt 控制命令。舵机控制继续使用：

- 误差死区；
- 命令限频；
- 最大角度步长；
- Pan/Tilt 安全边界；
- 丢脸保持和超时保护。

舵机控制异常不能反过来阻塞视频接收线程。控制命令必须是非阻塞或限频发送。

### 15.6 性能问题判定表

| 测试结果 | 优先怀疑方向 |
|---|---|
| 纯推流就卡 | ESP32 分辨率、JPEG 大小、云中继、WebSocket 接收或解码 |
| 纯推流流畅，加手势后卡 | 手势推理、关键点绘制或识别队列 |
| 手势流畅，加 InsightFace 后卡 | InsightFace 推理、Provider、模型输入尺寸或推理频率 |
| 识别流畅，加标识后卡 | Qt 图像转换、缩放或重复绘制 |
| 视频流畅，加舵机后卡 | 控制命令阻塞、发送频率或 ESP32 主循环负载 |
| 开始正常，运行后逐渐卡死 | 队列积压、未释放帧、Qt 信号堆积或连接关闭异常 |

### 15.7 当前优先级

当前优先实现阶段 A 的“肩载纯推流测试”入口。该入口不经过莲心 LLM，不启用人脸识别、手势识别和舵机控制，只用于建立远程视频基线。阶段 A 通过后，才继续实现阶段 B 的远程手势识别适配。
